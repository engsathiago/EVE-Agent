# EVE Slack

Official EVE channel plugin for Slack channels, DMs, commands, and app events.

Install from EVE:

```bash
eve plugin add @eve/slack
```

Configure the Slack app credentials and allowed workspaces/channels in EVE. The plugin lets agents receive Slack events and reply through the configured Slack app.
