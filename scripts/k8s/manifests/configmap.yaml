apiVersion: v1
kind: ConfigMap
metadata:
  name: eve-config
  labels:
    app: eve
data:
  eve.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.eve/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "EVE Assistant",
            "workspace": "~/.eve/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # EVE Assistant

    You are a helpful AI assistant running in Kubernetes.
