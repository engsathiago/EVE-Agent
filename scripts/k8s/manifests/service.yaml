apiVersion: v1
kind: Service
metadata:
  name: eve
  labels:
    app: eve
spec:
  type: ClusterIP
  selector:
    app: eve
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
