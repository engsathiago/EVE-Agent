<shortcuts xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <capability
        android:name="custom.actions.intent.ASK_EVE"
        app:queryPatterns="@array/ask_eve_query_patterns">
        <intent
            android:action="ai.eve.app.action.ASK_EVE"
            android:targetPackage="ai.eve.app"
            android:targetClass="ai.eve.app.MainActivity">
            <parameter
                android:name="prompt"
                android:key="prompt"
                android:mimeType="https://schema.org/Text"
                android:required="true" />
        </intent>
    </capability>
</shortcuts>
